{
    "name": "exact-auth-gateway",
    "version": "1.0.0",
    "description": "Authentication gateway for Exact Online API integration with Azure Data Factory",
    "main": "src/functions/*.js",
    "scripts": {
        "start": "func start",
        "test": "echo \"No tests specified\" && exit 0"
    },
    "dependencies": {
        "@azure/functions": "^4.0.0",
        "@azure/identity": "^4.0.0",
        "@azure/keyvault-secrets": "^4.7.0",
        "axios": "^1.6.0",
        "dotenv": "^16.5.0"
    },
    "devDependencies": {
        "azure-functions-core-tools": "^4.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
